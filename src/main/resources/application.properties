
#DB Config

spring.datasource.url=jdbc:mysql://localhost:3306/blog_app_apis?useSSL=false&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password = root
spring.datasource.hikari.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.MySQL8Dialect


spring.application.name=app-blog-apis
server.port=9090


spring.servlet.multipart.max-file-size = 10MB
spring.servlet.multipart.max-request-size=10MB
project.image = images/

logging.level.org.springframework.security = DEBUG

#spring.security.user.name=akshat
#spring.security.user.password=akshat
#spring.security.user.roles=ADMIN

#server.servlet.context-path=/address-app/api

#eureka.instance.metadata-map.contextpath=/address-app/api

#In order to made the micro services to register with Eureka if it is running on port other than 8761 then below property needs to be changed by providing hte actula URL for Eureka Server

#eureka.client.service-url.defaultZone = http://localhost:5000/eureka/
